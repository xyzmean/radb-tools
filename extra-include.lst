# Extra domains and IPs to INCLUDE in the final RU+CN prefix list.
# Supported formats:
#   domain name   — resolved via DNS at generation time
#   IPv4 address  — e.g. 8.8.8.8
#   CIDR subnet   — e.g. 1.1.1.0/24
#   AS number     — e.g. AS45102 (resolved to announced prefixes via RIPE API)

txxx.porn
aliexpress.ru
AS45102
AS32590
google.com
google.ru
2ip.ru
2ip.io
# Hetzner range hosting 2ip.ru/2ip.io — explicit fallback if DNS resolution fails on CI
188.40.167.0/24
